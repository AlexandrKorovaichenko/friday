
import { Dispatch } from 'redux';

export const initialState = {}
export const allReducer = (state = initialState, action: any) : any=> {
    switch (action.type) {
        
        default: return state;

        }
}
